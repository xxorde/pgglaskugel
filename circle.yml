dependencies:
  pre:
    - sudo apt update; sudo apt install zstd
  override:
  post:
    - make pgglaskugel
    - make man
    - make tarball 
    - cp pgglaskugel pgGlaskugel.tar.xz $CIRCLE_ARTIFACTS/

test:
  override:
    - make test
